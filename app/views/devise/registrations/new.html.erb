<div class="show-user-container">
  <%= render "shared/topnavbar" %>
  <div class="form-title">Sign up</div>
  <div class="devise-content-container" data-controller="places" data-places-api-key-value="<%= ENV['GOOGLE_MAPS_API_KEY'] %>">
    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>

      <div class="form-inputs">
        <div class= "form-row">
          <%= f.input :email,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "email" }%>
        </div>
        <div class= "form-row">
          <%= f.input :first_name,
              required: true,
              autofocus: true,
              input_html: { autocomplete: "first_name" }%>
        </div>
        <div class= "form-row">
          <%= f.input :last_name,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "last_name" }%>
        </div>
        <div class= "form-row">
          <%= f.input :username,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "username" }%>
        </div>
        <div class= "form-row">
          <%= f.input :location,
                      required: false,
                      autofocus: true,
                      input_html: { autocomplete: "location", data: {target: "places.input"}},  data: { action: "keydown->places#initializeAutocomplete"}%>
        </div>
        <div class= "form-row">
          <%= f.input :password,
                      required: true,
                      hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                      input_html: { autocomplete: "new-password" } %>
        </div>
        <div class= "form-row">
          <%= f.input :password_confirmation,
                      required: true,
                      input_html: { autocomplete: "new-password" } %>
        </div>
      </div>

      <div class="log-in-btn">
        <%= f.button :submit, "Sign up", class: "btn btn-blue" %>
      </div>
    <% end %>
    <%= render "devise/shared/links" %>
  </div>
</div>
