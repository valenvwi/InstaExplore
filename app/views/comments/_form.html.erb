<%= simple_form_for([@post, @comment], data: {insert_in_list_target: "form", action: "submit->insert-in-list#send"}) do |f| %>
  <%= f.input :content, label: "Leave a comment" %>
  <%= f.button :submit, "Submit", class:"btn-blue" %>
<% end %>
